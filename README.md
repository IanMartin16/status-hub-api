# Status Hub API

Servicio ligero para monitorear el estado de servicios de Evilink y exponer una salida pública para widgets de Service Health.

## Requisitos
- Python 3.11+

## Instalación
```bash
python -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
cp .env.example .env